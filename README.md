# Learn New Stack

Static landing page for curated learning repositories. Site content lives in [`data/`](data/README.md) — see that README to add repos or categories.

```bash
node scripts/validate-data.mjs
```
